# resultados e logs locais (a entrega é a tabela no chat)
outputs/

# ambiente virtual Python
.venv/

# caches Python
__pycache__/
*.pyc
.pytest_cache/

# GSD: hook wiring is environment-specific (hardcoded node path) — regenerate per machine via `npx @opengsd/gsd-core --claude --local`
.claude/settings.local.json
